name: devops
type: definition
description: CI/CD, deployment, infrastructure, monitoring, incident response
mission: Keep systems running, deployable, and observable at all times

capabilities:
  - ci-cd
  - deployment
  - docker
  - monitoring
  - incident-response
  - infrastructure
  - security-hardening

mode: act
backend: claude
constraints:
  max_turns: 15
  approval_required: true