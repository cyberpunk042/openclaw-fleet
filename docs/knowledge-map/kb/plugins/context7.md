# context7

**Type:** Claude Code Plugin
**Source:** @upstash/context7-mcp (also available as MCP server)
**Assigned to:** ENG, ARCH, WRITER

## Purpose

Up-to-date library/framework documentation. Prevents hallucinated APIs by fetching current docs directly. 47K stars.

## Configuration

Optional API key for higher rate limits. Plugin injects context automatically; MCP server provides on-demand query tools.

## Relationships

foundation-deps skill (dependency decisions), Context7 MCP server (complementary — plugin=injection, MCP=query), investigation stage (library research)
